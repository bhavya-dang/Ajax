const Discord = require("discord.js");

module.exports.run = async (bot, message, args) => {
  message.channel.send("No worries. @Admin is on the way!!:thumbsup: ")
}

module.exports.help = {
  name: "help"
}
